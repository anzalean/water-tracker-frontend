// src/components/Logo/Logo.jsx

import styles from "./Logo.module.css";

const Logo = () => {
  return (
    <a href="/" className={styles.logo}>
      AQUATRACK
    </a>
  );
};

export default Logo;
